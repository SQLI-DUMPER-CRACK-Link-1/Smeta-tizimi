import React from'react';import type{DesignReview}from'./types';export function DesignReviewCard({review,onOpen,onSubmit}:{review:DesignReview;onOpen?:(reviewId:string)=>void;onSubmit?:(reviewId:string)=>void}){return <section className="rounded-2xl border border-white/10 bg-white/[.03] p-5"><div className="flex justify-between"><div><div className="text-xs tracking-widest text-fuchsia-300">DESIGN CONTROL</div><b className="text-white">{review.discipline} review</b></div><span className="text-xs text-slate-300">{review.status}</span></div><div className="mt-3 text-sm text-slate-400">Document: {review.documentId} · Revision: {review.revisionId} · Comments: {review.commentsCount}</div><div className="mt-4 flex gap-2"><button onClick={()=>onOpen?.(review.reviewId)} className="rounded border border-white/15 px-3 py-2 text-sm">Open review</button><button onClick={()=>onSubmit?.(review.reviewId)} disabled={review.status!=='DRAFT'} className="rounded bg-fuchsia-500/20 px-3 py-2 text-sm disabled:opacity-40">Submit review</button></div></section>}
