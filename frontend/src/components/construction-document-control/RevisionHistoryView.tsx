import type { RevisionEvent } from '../../lib/construction-document-control';
export function RevisionHistoryView({events}:{events:readonly RevisionEvent[]}){return <section aria-label="Revision history" className="rounded-xl border border-white/10 p-4"><h2 className="font-medium">Revision / tarix</h2><ol className="mt-3 space-y-2 border-l border-white/15 pl-4">{events.map(e=><li key={e.revisionId}><b>{e.revisionId}</b> · {e.kind} · {e.status}{e.immutable&&<span className="ml-2 text-emerald-300">immutable</span>}<p className="text-sm text-slate-400">{e.reason} · {e.actorId??'—'} · {e.occurredAt??'—'}</p></li>)}</ol></section>}
