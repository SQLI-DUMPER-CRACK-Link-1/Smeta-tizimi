import type { RevisionEvent } from '../../lib/construction-document-control';
const KIND_LABEL: Record<RevisionEvent['kind'], string> = { baseline: 'Boshlang‘ich smeta', change: 'Tasdiqlangan o‘zgarish', certification: 'F2 tasdig‘i' };
const STATUS_LABEL: Record<string, string> = { approved: 'Tasdiqlangan', rejected: 'Rad etilgan', pending: 'Kutilmoqda', certified: 'Muzlatilgan davr' };
export function RevisionHistoryView({events}:{events:readonly RevisionEvent[]}){return <section aria-label="O‘zgarishlar va tasdiqlar tarixi" className="rounded-xl border border-white/10 p-4"><h2 className="font-medium">O‘zgarishlar va tasdiqlar tarixi</h2><ol className="mt-3 space-y-2 border-l border-white/15 pl-4">{events.map((e,index)=><li key={e.revisionId}><b>{KIND_LABEL[e.kind]}</b> · {STATUS_LABEL[e.status] ?? 'Holat qayd etilgan'}<span className="ml-2 text-emerald-300">{e.immutable ? 'Tarix o‘zgarmaydi' : ''}</span><p className="text-sm text-slate-400">{e.reason}{e.occurredAt ? ` · ${e.occurredAt}` : ''}{!e.reason && !e.occurredAt ? ` · ${index + 1}-qayd` : ''}</p></li>)}</ol></section>}
