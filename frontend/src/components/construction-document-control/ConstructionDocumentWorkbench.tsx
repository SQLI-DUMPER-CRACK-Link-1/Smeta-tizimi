import { calculateProgressValuation, createExportPreview, exportExceptions, validateProjectCloseout, type ConstructionDocumentControlReadModel, type ProgressValuationPage } from '../../lib/construction-document-control';
import { ChangeControlWorkspace } from './ChangeControlWorkspace'; import { ExportPreview, ExportValidationSummary, ReconciliationErrors } from './ExportPreview'; import { NakopitelniyWorkspace } from './NakopitelniyWorkspace'; import { BlockingIssuesPanel, MissingDocumentsPanel, PeriodReconciliationPanel, ProjectCloseoutMatrix } from './ProjectCloseoutWorkspace'; import { ProgressValuationWorkspace } from './ProgressValuationWorkspace'; import { RevisionHistoryView } from './RevisionHistoryView';
/** Composition-only workbench: caller gets data through generic typed ports. */
export function ConstructionDocumentWorkbench({model,page}:{model:ConstructionDocumentControlReadModel;page:ProgressValuationPage}){
  if (model.valuation.periods.length === 0) return <main className="space-y-5" aria-label="Construction document control workbench"><header><p className="text-xs text-cyan-300">PTO DOCUMENT CONTROL</p><h1 className="text-xl font-semibold">{model.projectName} · {model.objectName}</h1><p className="text-sm text-slate-400">Contract: {model.contractId??'—'} · Estimate: {model.valuation.estimateRevisionId}</p></header><section className="rounded-xl border border-white/10 p-4"><h2 className="font-medium">Tasdiqlangan F2 davri yo‘q</h2><p className="mt-2 text-sm text-slate-400">Bu obyekt uchun hali tasdiqlangan F2 davri mavjud emas. Nakopitelniy va davr eksporti tasdiqlangan F2 paydo bo‘lgach ochiladi.</p></section></main>;
  const valuation=calculateProgressValuation(model.valuation), closeout=validateProjectCloseout(model), issues=exportExceptions(model), preview=createExportPreview(model);return <main className="space-y-5" aria-label="Construction document control workbench"><header><p className="text-xs text-cyan-300">PTO DOCUMENT CONTROL</p><h1 className="text-xl font-semibold">{model.projectName} · {model.objectName}</h1><p className="text-sm text-slate-400">Contract: {model.contractId??'—'} · Estimate: {model.valuation.estimateRevisionId} · Period: {model.currentPeriodId??'—'}</p></header><BlockingIssuesPanel issues={issues}/><ProgressValuationWorkspace page={page}/><NakopitelniyWorkspace result={valuation}/><ChangeControlWorkspace changes={model.valuation.changes}/><RevisionHistoryView events={model.revisions}/><ProjectCloseoutMatrix rows={closeout}/><MissingDocumentsPanel rows={closeout}/><PeriodReconciliationPanel rows={closeout}/><ExportPreview model={preview}/><ExportValidationSummary errors={preview.reconciliation}/><ReconciliationErrors errors={preview.reconciliation}/><section className="rounded border border-amber-500/30 p-3 text-sm">Payment/certification rule pack: <b>FORMA3_RULE_UNRESOLVED</b>. Legal totals are intentionally unavailable.</section></main>}
