import {describe,it,expect} from 'vitest'; import {availableBytes,quotaStatus,uploadDecision,usagePercent} from './quota';
describe('storage quota byte accounting',()=>{it('calculates exact reserved capacity',()=>{expect(availableBytes(100,92,5)).toBe(3);expect(usagePercent(92,5,100)).toBe(97)});it('uses deterministic thresholds',()=>{expect(quotaStatus(79.9)).toBe('NORMAL');expect(quotaStatus(80)).toBe('WARNING');expect(quotaStatus(90)).toBe('CRITICAL');expect(quotaStatus(100)).toBe('HARD_LIMIT')});it('blocks before upload and subscription expiry',()=>{expect(uploadDecision(100,97,1,3,'ACTIVE')).toBe('BLOCKED_QUOTA');expect(uploadDecision(100,1,0,1,'EXPIRED')).toBe('BLOCKED_SUBSCRIPTION')});it('returns warning for incoming capacity',()=>expect(uploadDecision(100,79,0,2,'ACTIVE')).toBe('WARNING'))});
