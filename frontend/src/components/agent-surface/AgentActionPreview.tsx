import React from'react';import type{AgentAction}from'./types';export function AgentActionPreview({action,onApprove,onReject}:{action:AgentAction;onApprove?:(actionId:string)=>void;onReject?:(actionId:string)=>void}){return <section className="rounded-2xl border border-white/10 bg-white/[.03] p-5"><div className="text-xs tracking-widest text-indigo-300">AI ACTION PREVIEW</div><h2 className="mt-1 text-white">{action.tool}</h2><p className="mt-2 text-sm text-slate-300">{action.summary}</p><p className="mt-3 text-xs text-slate-400">Mode: {action.mode} · Evidence: {action.evidenceRefs.join(', ')||'none'} · Agent: {action.agentId}</p>{action.mode==='APPROVAL_REQUIRED'&&<div className="mt-4 flex gap-2"><button onClick={()=>onApprove?.(action.actionId)} className="rounded bg-emerald-500/20 px-3 py-2 text-sm">Approve</button><button onClick={()=>onReject?.(action.actionId)} className="rounded border border-white/15 px-3 py-2 text-sm">Reject</button></div>}</section>}
