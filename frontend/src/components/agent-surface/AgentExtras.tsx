import React from'react';import type{AgentAction}from'./types';export const AgentPanel=({actions}:{actions:AgentAction[]})=><ul>{actions.map(x=><li key={x.actionId}>{x.tool}</li>)}</ul>;export const AgentPermissionBadge=({mode}:{mode:string})=><span>{mode}</span>;export const AgentWriteApproval=({actionId,onApprove}:{actionId:string;onApprove?:(id:string)=>void})=><button onClick={()=>onApprove?.(actionId)}>Approve</button>;export const AgentEvidencePanel=({refs}:{refs:string[]})=><ul>{refs.map(r=><li key={r}>{r}</li>)}</ul>;
