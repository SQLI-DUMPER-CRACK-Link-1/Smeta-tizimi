import React from'react';import type{WorkforceRecord}from'./types';export function WorkforceCard({record,onOpen}:{record:WorkforceRecord;onOpen?:(employeeId:string)=>void}){return <section className="rounded-2xl border border-white/10 bg-white/[.03] p-5"><div className="text-xs tracking-widest text-sky-300">HR / ATTENDANCE / EQUIPMENT</div><div className="mt-2 flex justify-between"><b className="text-white">{record.name}</b><span className="text-xs text-slate-300">{record.attendanceStatus}</span></div><p className="mt-2 text-sm text-slate-400">{record.role} · employee {record.employeeId} · attendance {record.attendanceId??'—'}</p><p className="mt-2 text-sm text-slate-400">Equipment: {record.equipmentId??'—'} {record.equipmentStatus??''}</p><button onClick={()=>onOpen?.(record.employeeId)} className="mt-4 rounded border border-white/15 px-3 py-2 text-sm">Open record</button></section>}
