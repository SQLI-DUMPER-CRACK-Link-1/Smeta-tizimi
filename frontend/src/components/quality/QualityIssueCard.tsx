import React from'react';import type{QualityIssue}from'./types';export function QualityIssueCard({issue,onOpen,onClose}:{issue:QualityIssue;onOpen?:(issueId:string)=>void;onClose?:(issueId:string)=>void}){return <section className="rounded-2xl border border-white/10 bg-white/[.03] p-5"><div className="flex justify-between"><div><div className="text-xs tracking-widest text-amber-300">{issue.category}</div><b className="text-white">{issue.title}</b></div><span className={issue.status==='OVERDUE'?'text-red-300':'text-amber-200'}>{issue.status}</span></div><p className="mt-3 text-sm text-slate-400">Issue {issue.issueId} · inspection {issue.inspectionId??'—'} · assignee {issue.assigneeId??'—'}</p><p className="mt-2 text-sm text-slate-300">Corrective action: {issue.correctiveAction??'Not specified'}</p><div className="mt-4 flex gap-2"><button onClick={()=>onOpen?.(issue.issueId)} className="rounded border border-white/15 px-3 py-2 text-sm">Open</button><button onClick={()=>onClose?.(issue.issueId)} disabled={issue.status==='CLOSED'} className="rounded bg-emerald-500/20 px-3 py-2 text-sm disabled:opacity-40">Close issue</button></div></section>}
