import{describe,it,expect}from'vitest';import type{CommercialContract}from'./types';describe('commercial identity',()=>it('uses contract and party ids',()=>{const c:CommercialContract={contractId:'contract-1',companyId:'company-1',number:'C-1',scope:'scope',currency:'UZS',contractValue:1,parties:[{partyId:'party-1',companyId:'company-1',legalName:'X',role:'CUSTOMER'}],changeOrders:[],payments:[]};expect(c.contractId).toBe('contract-1')}));
