import { X } from 'lucide-react'; import { ParticipantCard } from './ParticipantCard'; import type { ProjectParticipant } from './types';
export function ParticipantDetailsDrawer({ participant, onClose, onSuspend, onRemove }: { participant: ProjectParticipant | null; onClose: () => void; onSuspend?: (p: ProjectParticipant) => void; onRemove?: (p: ProjectParticipant) => void }) { if (!participant) return null; return <aside role="dialog" aria-label="Participant details" className="fixed inset-y-0 right-0 z-50 w-full max-w-md border-l border-border bg-surface p-5 shadow-2xl"><div className="flex justify-between"><div><p className="text-xs text-text-dim">ISHTIROKCHI TAFSILOTI</p><h2 className="mt-1 text-lg font-semibold">{participant.company}</h2></div><button aria-label="Drawer yopish" onClick={onClose}><X /></button></div><div className="mt-5"><ParticipantCard participant={participant} /></div><div className="mt-5 flex gap-2"><button onClick={() => onSuspend?.(participant)} className="rounded border border-warn/40 px-3 py-2 text-xs text-warn">Suspend</button><button onClick={() => onRemove?.(participant)} className="rounded border border-danger/40 px-3 py-2 text-xs text-danger">Remove</button></div></aside>; }
