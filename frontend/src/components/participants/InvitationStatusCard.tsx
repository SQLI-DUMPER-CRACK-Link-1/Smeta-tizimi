import { Mail, RotateCw, X } from 'lucide-react'; import type { ParticipantInvitation } from './types'; import { ParticipantRoleBadge } from './ParticipantBadges';
export function InvitationStatusCard({ invitation,onResendInvite,onCancelInvite }: { invitation: ParticipantInvitation; onResendInvite?: (i:ParticipantInvitation)=>void; onCancelInvite?: (i:ParticipantInvitation)=>void }) { return <article className="rounded-lg border border-border bg-surface-2/35 p-3"><div className="flex justify-between gap-2"><div><p className="font-semibold text-sm">{invitation.company}</p><p className="mt-1 flex gap-1 text-xs text-text-dim"><Mail size={12}/>{invitation.emailOrIdentifier}</p></div><span className={`text-xs font-medium ${invitation.state==='sent'?'text-warn':invitation.state==='failed'||invitation.state==='expired'?'text-danger':'text-text-dim'}`}>{invitation.state.toUpperCase()}</span></div><div className="mt-2"><ParticipantRoleBadge role={invitation.role}/></div><div className="mt-3 flex gap-2"><button onClick={()=>onResendInvite?.(invitation)} className="inline-flex items-center gap-1 rounded border border-border px-2 py-1 text-xs"><RotateCw size={12}/>Qayta yuborish</button><button onClick={()=>onCancelInvite?.(invitation)} className="inline-flex items-center gap-1 rounded border border-danger/40 px-2 py-1 text-xs text-danger"><X size={12}/>Bekor</button></div></article>; }
