import { Building2, Eye, Mail, ShieldCheck } from 'lucide-react';
import { ParticipantRoleBadge, ParticipantStatusBadge } from './ParticipantBadges';
import type { ProjectParticipant } from './types';
export function ParticipantCard({ participant, onOpen }: { participant: ProjectParticipant; onOpen?: (participant: ProjectParticipant) => void }) { return <article className="karta p-4"><div className="flex justify-between gap-3"><div className="min-w-0"><p className="flex items-center gap-1.5 text-[11px] uppercase tracking-wide text-text-dim"><Building2 size={13} />Tashkilot</p><h3 className="mt-1 truncate font-semibold">{participant.company}</h3></div><ParticipantStatusBadge status={participant.status} /></div><div className="mt-3"><ParticipantRoleBadge role={participant.role} /></div><div className="mt-4 space-y-1.5 text-xs text-text-dim"><p>{participant.joinedAt ? `Qo‘shilgan: ${participant.joinedAt}` : 'Qo‘shilish sanasi yo‘q'}</p>{participant.contact && <p className="flex gap-1"><Mail size={13} />{participant.contact}</p>}{participant.contractRelation && <p>{participant.contractRelation}</p>}<p className="flex gap-1"><ShieldCheck size={13} />{participant.permissions?.join(' · ') || 'Ruxsatlar aniqlanmagan'}</p></div><button onClick={() => onOpen?.(participant)} className="mt-4 inline-flex items-center gap-1.5 rounded border border-border px-2.5 py-1.5 text-xs hover:bg-surface-2"><Eye size={13} />Tafsilot</button></article>; }
