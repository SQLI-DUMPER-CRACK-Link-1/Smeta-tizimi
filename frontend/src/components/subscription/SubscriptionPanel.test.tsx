import{describe,it,expect}from'vitest';import type{CompanySubscription}from'./types';describe('subscription contract',()=>it('uses company id as identity',()=>{const s:CompanySubscription={companyId:'c-1',planCode:'pro',planLabel:'Pro',status:'ACTIVE',entitlements:[],usage:{seatsUsed:1,seatsLimit:2,projectsUsed:1,projectsLimit:3,storageUsedBytes:1,storageQuotaBytes:2}};expect(s.companyId).toBe('c-1')}));
