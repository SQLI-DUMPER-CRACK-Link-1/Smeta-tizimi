import React from'react';import type{ScheduleTask}from'./types';export const ActivityTable=({items}:{items:ScheduleTask[]})=><ul>{items.map(x=><li key={x.taskId}>{x.name}</li>)}</ul>;export const MilestoneStrip=ActivityTable;export const DependencyList=({task}:{task:ScheduleTask})=><div>{task.dependencyTaskIds.join(',')}</div>;export const ScheduleStatusBadge=({status}:{status:string})=><span>{status}</span>;export const DelayRiskCard=({taskId,risk}:{taskId:string;risk:string})=><aside>{taskId}:{risk}</aside>;export const BaselineVsActual=({task}:{task:ScheduleTask})=><div>{task.startDate}→{task.finishDate}</div>;export const LookaheadPanel=ActivityTable;export const ScheduleCenter=ActivityTable;
