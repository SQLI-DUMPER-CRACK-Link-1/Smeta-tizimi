import React from'react';import type{ScheduleTask}from'./types';export function ScheduleBoard({tasks,onOpen}:{tasks:ScheduleTask[];onOpen?:(taskId:string)=>void}){return <section className="rounded-2xl border border-white/10 bg-white/[.03] p-5"><div className="text-xs tracking-widest text-teal-300">NATIVE SCHEDULE</div><h2 className="mt-1 text-xl text-white">Loyiha jadvali</h2><div className="mt-4 space-y-3">{tasks.map(t=><button key={t.taskId} onClick={()=>onOpen?.(t.taskId)} className="w-full rounded border border-white/10 p-3 text-left"><div className="flex justify-between"><b>{t.name}</b><span className="text-xs text-slate-400">{t.status} · {t.progressPercent}%</span></div><div className="mt-2 h-1.5 rounded bg-white/10"><div className="h-full rounded bg-teal-400" style={{width:`${Math.max(0,Math.min(100,t.progressPercent))}%`}}/></div><div className="mt-2 text-xs text-slate-500">{t.startDate??'—'} → {t.finishDate??'—'} · dependencies: {t.dependencyTaskIds.length}</div></button>)}</div></section>}
