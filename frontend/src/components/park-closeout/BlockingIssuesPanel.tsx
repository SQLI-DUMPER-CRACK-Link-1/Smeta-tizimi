import type { ExportConsistencyIssue } from '../../lib/park-closeout';
export function BlockingIssuesPanel({ issues }: { issues: readonly ExportConsistencyIssue[] }) { return <section aria-label="Closeout blocking issues" className="rounded-xl border border-red-500/30 p-4"><h2 className="font-medium">Bloklovchi eksport xatolari</h2>{issues.length ? <ul className="mt-2 space-y-1 text-sm text-red-200">{issues.map(issue => <li key={`${issue.periodId}:${issue.rule}`}>• {issue.periodId} · {issue.rule}: {issue.reason}</li>)}</ul> : <p className="mt-2 text-sm text-emerald-300">Eksport invariantlari buzilmagan.</p>}</section>; }
