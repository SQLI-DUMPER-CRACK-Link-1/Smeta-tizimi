import type { CloseoutReportRow } from '../../lib/park-closeout';
export function MissingDocumentsPanel({ rows }: { rows: readonly CloseoutReportRow[] }) { const missing = rows.filter(row => ['missing', 'superseded', 'unresolved'].includes(row.status)); return <section aria-label="Yetishmayotgan hujjatlar" className="rounded-xl border border-amber-500/30 p-4"><h2 className="font-medium">Yetishmayotgan / aniqlanmagan hujjatlar</h2>{missing.length ? <ul className="mt-2 space-y-1 text-sm">{missing.map(row => <li key={row.requirement}>• {row.requirement}: {row.reason}</li>)}</ul> : <p className="mt-2 text-sm text-emerald-300">Yetishmayotgan talab yo‘q.</p>}</section>; }
