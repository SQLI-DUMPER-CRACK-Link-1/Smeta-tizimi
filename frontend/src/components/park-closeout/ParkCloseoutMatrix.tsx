import type { CloseoutReportRow } from '../../lib/park-closeout';
export function ParkCloseoutMatrix({ rows }: { rows: readonly CloseoutReportRow[] }) { return <section aria-label="PARK closeout matrix" className="overflow-x-auto rounded-xl border border-white/10 p-4"><table className="w-full text-left text-sm"><thead className="text-xs text-slate-400"><tr><th>Talab</th><th>Holat</th><th>Blok</th><th>Dalil</th><th>Sabab</th></tr></thead><tbody>{rows.map(row => <tr key={row.requirement} className="border-t border-white/5"><td className="py-2">{row.requirement}</td><td>{row.status}</td><td className={row.blocking ? 'text-red-300' : 'text-emerald-300'}>{row.blocking ? 'HA' : 'YO‘Q'}</td><td className="font-mono text-xs">{row.evidenceIds.join(', ') || '—'}</td><td>{row.reason}</td></tr>)}</tbody></table></section>; }
