# Cloudflare Pages `npm clean-install` ishlatadi va peer-dependency
# nomuvofiqligida butun build'ni to'xtatadi. Lokalda o'rnatish boshqacha
# ketgani uchun farq chiqadi. Bu sozlama ikkalasini bir xil qiladi.
legacy-peer-deps=true
audit=false
fund=false
