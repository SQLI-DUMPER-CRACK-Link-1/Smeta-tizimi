# T2 БЕЗСКЛАД SQL semantic audit

> **Parent reconciliation (2026-09-11, post-audit):** The findings below describe the initial read-only snapshot. Since then, the parent added deterministic SQL normalization for full-width/combining inputs (`20261021150000`), added the final migration-order pin `20261025100000` for the chunked writer, added `20261025090000` for object nakrutka’s `bez/mk` buckets, and added `t2_nakrutka` acceptance coverage. Local SQL execution remains **BLOCKED** because no PostgreSQL/Supabase runtime is installed. Existing-row backfill/downstream-total review and live DB/RLS readback remain open.

Scope: `supabase/migrations/20261021150000_t2_resurs_kategoriya_bezsklad_v1.sql` and `supabase/tests/t2_resurs_kategoriya_bezsklad_v1.acceptance.sql`, compared with the repository migrations and the read-only production schema inventory. No SQL was applied; no source or Git state was changed.

1. **BLOCKED — syntax/runtime execution.** `psql`, `supabase`, and `pg_isready` were unavailable, so neither migration nor acceptance SQL was parsed or executed. Static review found no certain grammar error, but executable/syntax status remains unproven. The acceptance script is explicitly post-migration and source-only (`acceptance.sql:3-6`).

2. **PROVEN/PARTIAL — dependency and constraints.** The prerequisite registry migration creates `t2_resurs_kategoriya`, its old six-value CHECK, key functions, unique key, and RLS (`20261013090000_t2_resurs_kategoriya_registry_v1.sql:30-62`). The target widens the registry and `t2_qator` vocabularies (`20261021150000...:95-144`, `:151-208`). The captured schema has the old `t2_qator_kat_check`, matching the target’s legacy-check detector (`supabase/baseline/inventory/constraints.json:1`). The captured production inventory has no `t2_resurs_kategoriya` object (`supabase/baseline/inventory/objects.json:1`), so applying the target there before the registry migration is intentionally **BLOCKED** by `:95-97`.

3. **PROVEN/PARTIAL — transaction safety.** The migration encloses DDL/function replacement and constraint changes in one transaction (`20261021150000...:24`, `:484`); its explicit prechecks raise instead of silently rewriting invalid category data (`:99-104`, `:161-167`). The acceptance DML is transaction-local and rolls back (`acceptance.sql:15`, `:291`). Runtime lock/rollback behavior and compatibility with the project’s migration runner are **BLOCKED** because no database execution occurred.

4. **PROVEN/PARTIAL — tenant/auth boundary.** The writer preserves active-membership/role verification through `t2_actor_kompaniya_azo_tekshir` (`20261021150000...:226-230`, `:397-412`), whose repository signature and role return type match the inventory (`supabase/baseline/inventory/functions.json:1`; implementation `20260830052000_t2_company_storage_foundation_v1.sql:102-113`). Registry lookup is explicitly company-scoped (`20261021150000...:273-279`), and protected functions are granted only to `service_role` (`:81-82`, `:252-253`, `:288-289`, `:479-482`). **PARTIAL:** there is no `auth.uid()` binding; the trusted caller supplies `p_actor_id`, and the acceptance does not test cross-tenant negative cases.

5. **PARTIAL — idempotency/concurrency.** Sequential replay is implemented through `operation_id` lookup and the global command-log primary key (`20261021150000...:222-249`; ledger definition `20260905120000_t2_company_onboarding_v1.sql:18-25`). The two import writers retain the same pre-check/log-at-end pattern (`:309-310`, `:381-382`, `:393-395`, `:472-473`). No advisory lock or equivalent exists around the check-and-write sequence; concurrent duplicate calls can race and one can end in a unique-key error rather than returning the prior result. The acceptance checks source text only (`acceptance.sql:39-70`) and does not exercise replay/concurrency.

6. **PROVEN/PARTIAL — category precedence.** The helper implements: unit-derived `ЧЕЛ/МАШ`, then name-derived `БЕЗСКЛАД`, then tenant registry, then `t2_kat_birlik` (`20261021150000...:262-280`). The repository’s `t2_kat_birlik(birlik text, nom text)` signature matches the call (`supabase/baseline/inventory/functions.json:1`), and the old import path was registry-then-fallback (`20261013090000_t2_resurs_kategoriya_registry_v1.sql:144-163`). The acceptance covers unit, name, fallback, and a registry-only case (`acceptance.sql:131-163`), but not the important conflict case where a confirmed registry row exists for a positive name; by design, the name classifier wins.

7. **MISSING — classifier parity.** The SQL allowlist matches the frontend token list (`20261021150000...:68-71`; `frontend/src/lib/resurs-kategoriya.ts:24-31`), but normalization is not equivalent. Frontend NFKD/mark removal accepts full-width and combining-mark forms (`frontend/src/lib/resurs-kategoriya.ts:88-100`; tests `frontend/src/lib/resurs-kategoriya.test.ts:23-25`), while SQL only uppercases and strips characters (`20261021150000...:35-39`), so those accepted frontend inputs can return NULL in SQL. Exclusion phrases also differ; the acceptance tests only plain ASCII/Cyrillic cases (`acceptance.sql:74-129`).

8. **MISSING — migration-chain preservation.** The target claims `20261021140000` is the latest migration (`20261021150000...:3-4`), but this checkout contains later `20261022090000`, `20261023090000`, and `20261024090000` migrations. `20261022090000` rewrites both import functions (`20261022090000_t2_smeta_import_rollup_fix_v1.sql:44-91`); then `20261023090000` fully replaces `t2_smeta_import_yakunla_v1` with the old registry/fallback expression and no `t2_resurs_kategoriya_import_kat_v1` call (`20261023090000_t2_smeta_import_yakunla_rollup_varname_fix_v1.sql:35-127`, especially `:70-75`). Thus, after the full current migration order, bulk and chunked import classifier behavior is not parity-preserved, and the acceptance source assertion for `yakunla` (`acceptance.sql:59-69`) should fail.

9. **MISSING — downstream/backfill coverage.** The target classifies only new import rows (`20261021150000...:328-335`, `:427-434`); it does not backfill existing `t2_qator` rows. The later object-total view still sums only `ЧЕЛ/МАШ/МАТ/ОБ` and has no `БЕЗСКЛАД` column (`20261024090000_t2_obyekt_jami_narxsiz_nol.sql:44-63`). The frontend classifier also intentionally leaves the legacy API category union unchanged (`frontend/src/lib/resurs-kategoriya.ts:4-6`). Existing rows and downstream totals/consumers therefore remain unproven or incomplete.

10. **BLOCKED/PARTIAL — acceptance executability and coverage.** The script is executable only after the prerequisite schema/function chain and under a privileged DB role: it assumes at least one company and object (`acceptance.sql:150-153`, `:173-176`, `:236-242`) and directly inserts into RLS-protected registry/qator tables. It rolls back its fixtures, but does not invoke the registry writer, does not test concurrent idempotency, does not test cross-tenant denial, and omits the registry-writer function from its privilege loop (`acceptance.sql:269-287`). Runtime pass/fail is **BLOCKED** without a database.

## Verdict

The target is structurally aligned with the registry migration and old qator constraint, and its intended precedence plus service-role boundary are visible in source. It is not runtime-approved: production prerequisite drift, frontend/SQL normalization mismatch, later migration overwrite of the chunked writer, missing backfill/downstream aggregation, and unexecuted acceptance SQL are release-blocking evidence gaps.
