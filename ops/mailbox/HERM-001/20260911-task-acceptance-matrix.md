# HERM-001 — 2026-09-11 task acceptance matrix

> **Parent reconciliation (2026-09-11, post-audit):** This matrix records the pre-reconciliation branch comparison below. The current worktree now has the seven-category classifier/UI/API/export integration, the final chunked-import migration pin, additive nakrutka bucket support, SQL acceptance scripts, and local verification: clean-install full Vitest `68/524`, focused LRV/classifier/import tests `137/137`, app/functions typecheck, lint, `npm run tekshir`, and governance all passed. PostgreSQL/Supabase runtime, authenticated browser smoke, real RES repricing, real ОБ object, production migration/deploy, destructive re-import, and main push remain **BLOCKED/NOT AUTHORIZED**.

**Scope:** read-only comparison of `TOPSHIRIQ_2026-09-11_CLAUDE_DAN.md` and the related HERM-001 governance/audit/mailbox records against the committed `origin/main` and task patch. This is an evidence/disposition record, not a merge, deploy, or production approval.

## Evidence basis

- **Current `origin/main`:** `5e7a1af2cccfc35f8255c5620b2eaf86e2525f7b` (HEAD points to the same commit).
- **Task patch:** `origin/task/20260911-claude-handoff` at `9a1b5b263c690702d6593c29f5c9dbef53ef17e6`.
- **Patch parent / merge base:** `6ebae58914fce627539aa4fcecc2461ec188c5a3`.
- `git rev-list --left-right --count origin/main...9a1b5b2` = **`31  1`**. The patch is therefore not based on the current `origin/main`; it requires a three-way review/rebase, not a blind merge.
- Patch-only diff (`9a1b5b2^..9a1b5b2`): **15 paths, 1,461 insertions, 94 deletions**. The overlapping `SmetaYuklaNative.tsx` and `lrv-plus-export.ts` versions are materially older than the current `origin/main` versions.
- Before this file was written, the worktree already contained unrelated/uncommitted source changes and untracked task files. They were preserved and not used as committed acceptance evidence. No source file, Git ref/index, or deployment was changed for this matrix; this file is the only requested output.

**Status vocabulary:** `VERIFIED LOCAL` means committed source/test evidence only; `PARTIAL` means implementation exists but a named acceptance gate is missing; `OWNER/PRODUCTION BLOCKED` means the requested action needs an owner decision, credentials/test data, DB/deploy authority, or real production validation; `INTEGRATION BLOCKED` means the patch cannot be accepted against current `origin/main` without reconciliation.

## Completion matrix (13 bands)

| # | Item / acceptance question | `origin/main` evidence | Task patch evidence and disposition | Status |
|---:|---|---|---|---|
| 1 | **Baseline and patch identity** | Current ref is `5e7a1af`; HERM-001 registry remains active on base `6ebae589` (`ops/ACTIVE_TASKS.json:662-720`). | Patch is remote-only `9a1b5b2`, based on `6ebae589`; `origin/main` is 31 commits ahead and the patch is 1 commit on the other side. | **INTEGRATION BLOCKED** |
| 2 | **2.1 — `БЕЗ СКЛАД` category and money semantics** | The task still records this as open: `T2ResursKategoriya` has only six values and zero `БЕЗ СКЛАД` rows (`TOPSHIRIQ...:76-138`; especially `:91-126`). Current exporter ordering is also six-category (`origin/main:frontend/src/lib/resurs-vedomost.ts:48`). | Patch adds the seventh type/UI allowlist and display order (`9a1b5b2:frontend/src/api/supabase.ts:1005-1008`, `.../resurs-vedomost.ts:48`, `.../lrv-plus-export.ts:72-76`). Its SQL path calls the category lookup from both import finalizers (`.../20261021150000...sql:334,433`). The pure TS classifier is not called by any other committed file in `9a1b5b2` (commit-wide search found only its own module and test). The patch migration is timestamped `20261021150000`, before the current documented latest migration `20261024090000` (`TOPSHIRIQ...:36-39`), so it would fail the current migration-order gate unless renumbered/reconciled. SQL runtime/DB acceptance was not executed (`9a1b5b2:ops/mailbox/HERM-001/20260911-hermes-lunch-checkpoint.md:49-59`). | **PARTIAL; INTEGRATION + DB/REAL-F2 BLOCKED** |
| 3 | **2.2 — existing-object re-import** | Current main has a non-destructive preview: it reads canonical rows, builds `smetaQaytaImportDiff`, and sets “write blocked” (`origin/main:frontend/src/admin/sahifalar/SmetaYuklaNative.tsx:809-827`). It has no committed `smeta_tozala` UI call. The related closure report explicitly says the destructive path was removed (`docs/audit/T2_PTO_CLOSURE_HERMES_REPORT_2026-09.md:70-76`). | Patch adds a confirmation dialog, calls `smeta_tozala`, and refuses deletion when dependent Fakt/price-basis data exists (`9a1b5b2:frontend/src/admin/sahifalar/SmetaYuklaNative.tsx:381-405`). This is a deliberate behavior conflict with the current non-destructive lane and is on the old 960-line file, versus 1,448 lines on current main. Owner approval is required before accepting a destructive path (`TOPSHIRIQ...:140-160`). | **PARTIAL; OWNER + REBASE BLOCKED** |
| 4 | **2.3 — PTO scope must not block export; provenance is optional** | The task records closure in `8b73a5f`: only object/context and complete read-model gate; provenance is optional and written to `МАНБА` when supplied (`TOPSHIRIQ...:162-191`). Current main has this gate and optional provenance (`origin/main:frontend/src/lib/lrv-plus-export.ts:110-177`). | The patch’s old exporter does not contain the current `RESURS_VEDOMOST`, `МАНБА`, or optional-provenance path; its output appends only `LRV_PLUS` (`9a1b5b2:frontend/src/lib/lrv-plus-export.ts:356-359`). The older HERMES report still describes provenance as required (`docs/audit/T2_PTO_CLOSURE_HERMES_REPORT_2026-09.md:62-68`), so the newer main/task decision must be retained during rebase. | **VERIFIED LOCAL ON CURRENT MAIN; PATCH MUST PRESERVE** |
| 5 | **2.4 — real RES repricing** | The open list gives the measured no-price counts: Amfiteatr2 **9,448**, Fast Food 1etaj **1,262**, Stella2 **187**, Karting2 **206** (`TOPSHIRIQ...:193-212`). Current UI can upload/index RES and apply only matching prices without overwriting an existing LRV price (`origin/main:frontend/src/admin/sahifalar/SmetaYuklaNative.tsx:1036-1045`). | No evidence of the owner’s actual RES files being processed, the two named objects being repriced, or totals being read back. The task explicitly prohibits copying another object’s price base. | **OWNER/PRODUCTION BLOCKED** |
| 6 | **2.5 — LRV Excel headers, widths, formats, freeze panes, export artifact** | Task says numeric formats/widths are done but headers/freeze were open (`TOPSHIRIQ...:214-241`). Current main now contains an ExcelJS post-process for frozen panes (`origin/main:frontend/src/lib/lrv-plus-export.ts:782-802`) and a local round-trip assertion (`origin/main:frontend/src/lib/lrv-plus-export.test.ts:183-196`). It also writes `RESURS_VEDOMOST` and optional `МАНБА` (`.../lrv-plus-export.ts:745-778`). | Patch adds seven category columns but emits only one `LRV_PLUS` sheet and has no current freeze/RES/provenance path (`9a1b5b2:frontend/src/lib/lrv-plus-export.ts:72-76,356-359`). It cannot replace current main wholesale. Document-specific header/visual acceptance and a real owner-generated file remain unproven. | **PARTIAL; REBASE + OWNER ARTIFACT REVIEW** |
| 7 | **2.6 — deleted object disappears from all lists/cache** | Task marks this done in `2665c47`: DB state was `holat='bekor'`, the stale result came from three independent caches, and `sinxronla()` refreshes local state, React Query, and PTO workspace (`TOPSHIRIQ...:243-269`; current code `origin/main:frontend/src/test02/TestObyektlar.tsx:168-180`). | Patch does not supersede this current-main fix. The remaining `Obyektlar.tsx:23` unused state is documented as low-priority dead code, not a closure blocker. | **VERIFIED LOCAL** |
| 8 | **2.7 — ОБ separation on a real file** | Static rule is documented: `ОБОРУДОВАНИЕ` section or the equipment footer percentage; `М/К` in the footer keeps the block as МАТ (`TOPSHIRIQ...:271-280`). | No real equipment-bearing object was tested. The owner confirmed Stella has no equipment, so Stella2 `ОБ=0` is not a validation case; the owner’s multi-object file can also make the footer fallback cross-contaminate blocks (`TOPSHIRIQ...:281-289`). | **PARTIAL; OWNER/REAL-FILE BLOCKED** |
| 9 | **2.8 — network, browser, authenticated smoke, self-check** | The task records both required hosts as sandbox-blocked and requires network allowlisting, a separate boss test account, and an etalon object (`TOPSHIRIQ...:291-314`). `CURRENT_STATE.md:20,37,157` and the HERMES report (`:123-130`) keep authenticated smoke/live export unproven. | In this audit the anonymous production root loaded at `https://smeta-tizimi.pages.dev/`, title **`SMETA TIZIM 02`**, viewport **`1264x625`**. This proves landing availability only. No authenticated session/cookie was used; no credential was requested or stored. Anonymous API/host probes do not establish authenticated role access, real import, repricing, export, or re-import. | **OWNER/NETWORK/PRODUCTION BLOCKED** |
| 10 | **Explicit owner decisions** | The open task still asks the owner to choose manual marking vs keyword automation for `БЕЗ СКЛАД` and to approve destructive re-import (`TOPSHIRIQ...:106-138,154-157`). It also says each object has its own RES price base (`:209-212`). | The patch checkpoint records a newer owner rule: name-based automation for `ВОДА/SUV`, `БЕТОН/BETON`, `РАСТВОР/RASTVOR`; `ПЕСОК/QUM`, `ЩЕБЕНЬ/SHEBEN`, `КВАРЦ/KVARS` remain stored materials; no fuzzy expansion (`9a1b5b2:ops/mailbox/HERM-001/20260911-hermes-lunch-checkpoint.md:12-17`). This is evidence of a recorded `(b)`-style decision, but it is not reconciled into the current task document/main and still needs owner/orchestrator acceptance before a money-sensitive merge. | **DECISION RECONCILIATION REQUIRED** |
| 11 | **Production freeze / write authority** | Constitution requires explicit human approval for production migration, destructive DDL/delete, deployment, and `main` push (`docs/governance/CONSTITUTION.md:41-50`). HERM-001 is `production_write_allowed:false` (`ops/ACTIVE_TASKS.json:662-721`). | Patch checkpoint states no production migration/deploy/destructive write (`9a1b5b2:ops/mailbox/HERM-001/20260911-hermes-lunch-checkpoint.md:3-10`). The related report remains `SOURCE_READY / PRODUCTION_BLOCKED` and says no production migration, deploy, destructive SQL/delete, or owner smoke was performed (`docs/audit/T2_PTO_CLOSURE_HERMES_REPORT_2026-09.md:1-10,123-150`). | **PRODUCTION BLOCKED / FREEZE IN FORCE** |
| 12 | **Local verification claims** | Current-main source/tests contain local evidence for the closed cache item and current export gate/freeze path, but no evidence here of authenticated or live RES/ОБ/re-import acceptance. | Patch checkpoint reports focused Vitest, full frontend Vitest, app/functions TypeScript, `npm run tekshir`, governance check, and `git diff --check` exit 0 (`9a1b5b2:ops/mailbox/HERM-001/20260911-hermes-lunch-checkpoint.md:49-57`). It also records a root-level `npx tsc -b` exit 1 and that SQL runtime acceptance was not executed (`:58-59`). These are checkpoint claims, not an independent post-commit CI attestation. | **LOCAL EVIDENCE PARTIAL; RERUN AFTER REBASE** |
| 13 | **Overall acceptance** | Current main has verified local closure for 2.3 and 2.6, and local export/freeze implementation evidence; the task document still marks 2.1, 2.2, 2.4, 2.5, 2.7, and 2.8 open/partial. | Patch contains useful source/test work, but its old base, migration-order violation, exporter regression risk, owner-decision discrepancy, absent SQL runtime test, and no real authenticated/RES/ОБ evidence prevent acceptance or deploy. | **NO-GO FOR MERGE/PRODUCTION** |

## Tasks that can be completed locally

1. Reconcile `9a1b5b2` onto current `origin/main` with a three-way review; preserve current PTO/provenance, RES export, freeze, and non-destructive import behavior unless the owner explicitly changes those decisions.
2. Renumber/rebase the `20261021150000` migration after the current `20261024090000` tail, then run the repository migration-order/governance/static gates and inspect the SQL against the live function signatures **without applying it**.
3. Decide one canonical `БЕЗСКЛАД` implementation (avoid two diverging TS/SQL classifiers), wire the accepted classifier through the actual T2 import path, and add/adjust local tests for the exact owner list, exclusions, category totals, export order, and nakrutka semantics.
4. Re-run the documented frontend TypeScript/Vitest/`npm run tekshir` checks after the three-way merge; resolve the recorded root-level `tsc -b` failure rather than treating the checkpoint as final.
5. Keep the current local cache fix and current export/provenance/freeze tests; add a local synthetic round-trip covering the final seven-category exporter without deleting current `RESURS_VEDOMOST`/`МАНБА` behavior.
6. Review the re-import UX locally (diff-only versus destructive clear), including dependent-data refusal and idempotency tests; do not exercise destructive deletion against production data.

## Owner / production-blocked tasks

1. Owner/orchestrator must reconcile the task document’s pending `БЕЗ СКЛАД` choice with the patch checkpoint’s narrow keyword rule and explicitly approve the money-sensitive semantics.
2. DB owner must review and, if approved, apply the renumbered additive migration in the correct controlled environment; run SQL runtime/RLS/grant/import acceptance with disposable data and read back the result.
3. Owner must provide/approve the real RES files and authorize repricing **Amfiteatr2** and **Fast Food 1etaj** using each object’s own price base; no cross-object SQL copying.
4. Owner must provide an equipment-bearing real object/file for ОБ validation and confirm the resulting split and nakrutka totals.
5. Network/production owner must allow the documented hosts, provide a separate boss-role test account and etalon object, and perform the authenticated vertical browser smoke (login → context/object → Smeta → RES/LRV → Fakt/F2 → export/re-import).
6. Owner approval is required before any destructive existing-smeta clear/re-import, production migration, production data write, deployment, or `main` push. Until then the release remains **NO-GO**.
