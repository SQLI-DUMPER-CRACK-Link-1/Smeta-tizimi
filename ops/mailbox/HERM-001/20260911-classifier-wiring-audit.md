# HERM-001 — БЕЗСКЛАД classifier wiring audit

> **Parent reconciliation (2026-09-11, post-audit):** This report’s initial `MISSING` verdict was correct for the pre-patch migration order. The additive `20261025100000_t2_resurs_kategoriya_import_final_v1.sql` now redefines `t2_smeta_import_yakunla_v1` with `t2_resurs_kategoriya_import_kat_v1`, and local source checks plus the frontend import/classifier tests pass. Live migration/readback and direct `t2_qator_qosh` behavior remain **BLOCKED** without a disposable Supabase runtime.

1. **[MISSING] Yakuniy hukm.** Name-based `БЕЗСКЛАД` helper mavjud va `bulk` writer’ga ulangan, lekin canonical yo‘lning amaldagi keyingi `t2_smeta_import_yakunla_v1` redefinition’i helperni olib tashlab, eski `registry → t2_kat_birlik` fallback’iga qaytadi. Shuning uchun “har bir canonical T2 import writer” talabi bajarilmagan: `supabase/migrations/20261021150000_t2_resurs_kategoriya_bezsklad_v1.sql:333-335,432-434` ga qarshi `supabase/migrations/20261023090000_t2_smeta_import_yakunla_rollup_varname_fix_v1.sql:65-83`.

2. **[PROVEN] SmetaYuklaNative kirish va haqiqiy import yo‘li.** UI `/api/smeta-yukla` ga POST qiladi (`frontend/src/admin/sahifalar/SmetaYuklaNative.tsx:36-48`), daraxtni flatten qilib `import_boshla → import_bolak → import_yakunla` ketma-ketligini chaqiradi (`.../SmetaYuklaNative.tsx:1068-1112`). `smetaDaraxtniYoy` payload’ga `local_id/parent/tur/kod/nom/birlik/hajm/narx/summa/norma` beradi, lekin `kat` bermaydi (`frontend/src/lib/smeta-flatten.ts:14-30,33-46`); demak import klassifikatsiyasi frontend payload’dan emas, server writer’dan kelishi kerak.

3. **[PROVEN; classifier wiring emas] Cloudflare endpoint forwarding.** `/api/smeta-yukla` auth’dan keyin barcha import amallarini dispatch qiladi (`frontend/functions/api/smeta-yukla.ts:215-237`); legacy one-shot yo‘l `t2_smeta_import_bulk_v1` ga (`:112-148`), chunked yo‘l esa `t2_smeta_import_boshla_v1`, `_bolak_v1`, `_yakunla_v1` ga (`:169-203`). Handler nom/klassifikatsiya qilmaydi, faqat `p_qatorlar`/kontekstni RPC’ga uzatadi; bu to‘g‘ri boundary, ammo helper ulanishi RPC’larning o‘ziga bog‘liq.

4. **[PROVEN] Bulk writer source wiring.** `t2_smeta_import_bulk_v1` ning classifier migration’dagi final body’si resurs (`rs/mat/ob`) uchun `t2_resurs_kategoriya_import_kat_v1(...)` ni chaqiradi (`supabase/migrations/20261021150000_t2_resurs_kategoriya_bezsklad_v1.sql:427-439`), so‘ng `kat` ni `t2_qator` ga yozadi (`:451-456`). Keyingi rollup migration bulk funksiyani dinamik qayta yaratadi, lekin faqat `t2_rollup` ni qo‘shadi (`supabase/migrations/20261022090000_t2_smeta_import_rollup_fix_v1.sql:70-91`); category call saqlanadigan source path bor.

5. **[MISSING] Chunked yakunla writer wiring.** Classifier migration avval chunked final writer’ni helperga ulagan (`supabase/migrations/20261021150000_t2_resurs_kategoriya_bezsklad_v1.sql:328-343`). Lekin keyingi `CREATE OR REPLACE` body’si `t2_resurs_kategoriya_import_kat_v1` ni chaqirmaydi; `kat` uchun faqat tenant registry va `t2_kat_birlik` ishlatadi (`supabase/migrations/20261023090000_t2_smeta_import_yakunla_rollup_varname_fix_v1.sql:65-83`). Aynan SmetaYuklaNative ishlatadigan chunked yo‘l (`import_yakunla`) uchun name-only `SUV/BETON/RASTVOR` kafolati shu sabab source-level’da yo‘q.

6. **[PROVEN] Boshla/bolak — staging, data writer emas.** `t2_smeta_import_boshla_v1` sessiya ochadi (`supabase/migrations/20261019090000_t2_smeta_import_bolakli_v1.sql:98-137`), `_bolak_v1` JSON chunk’ni `t2_smeta_import_bolak` ga upsert qiladi (`:139-172`). `t2_qator` ga canonical insert faqat `_yakunla_v1` ichida (`:237-243`); shuning uchun klassifikatsiya stage RPC’larda emas, final writer’da bo‘lishi kerak.

7. **[PROVEN] Owner rule va precedence helper.** `t2_resurs_kategoriya_import_kat_v1` tartibi aniq: unit-derived `ЧЕЛ/МАШ` birinchi (`supabase/migrations/20261021150000_t2_resurs_kategoriya_bezsklad_v1.sql:262-267`), name-derived `БЕЗСКЛАД` ikkinchi (`:268-270`), tenant registry uchinchi va `t2_kat_birlik` oxirgi (`:270-282`). SQL classifier faqat `ВОДА/SUV`, `БЕТОН/BETON`, `РАСТВОР/RASTVOR` tokenlarini qabul qiladi (`:68-76`), `ПЕСОК/QUM`, `ЩЕБЕНЬ/SHEBEN`, `КВАРЦ/KVARS` va storable concrete/dry-mix istisnolari bor (`:44-66`).

8. **[PARTIAL] Registry fallback va yozish kanali.** Registry jadvali nom+birlik bo‘yicha unique va dastlabki vocabulary’da `БЕЗСКЛАД` yo‘q edi (`supabase/migrations/20261013090000_t2_resurs_kategoriya_registry_v1.sql:30-40`); classifier migration CHECK’larni kengaytiradi (`supabase/migrations/20261021150000_t2_resurs_kategoriya_bezsklad_v1.sql:90-142,146-206`) va registry writer’ni yangi qiymat bilan qabul qiladi (`:214-253`). UI tasdiqlangan override’larni `/api/sb-yoz` orqali saqlaydi (`frontend/src/api/supabase.ts:1008-1022`; `frontend/functions/api/sb-yoz.ts:1408-1432`), ammo `SmetaYuklaNative` bu saqlashni best-effort qiladi va xato importni to‘xtatmaydi (`frontend/src/admin/sahifalar/SmetaYuklaNative.tsx:978-993`).

9. **[PROVEN helper / MISSING effective chunked path] ЧЕЛ/МАШ precedence.** Frontend RES classifier ham birlikni birinchi tekshiradi: `ЧЕЛ`/`МАШ` nom-tokenidan oldin qaytariladi (`frontend/src/admin/sahifalar/SmetaYuklaNative.tsx:388-406`), `katTaxmini` ham `ЧЕЛ`/`МАШ` ni alohida ajratadi (`:412-417`). Bu qoida SQL helper’da proven, lekin `20261023090000` dagi yakunla redefinition helperni chetlab o‘tib registry’ni avval qo‘llaydi (`:70-75`); demak effective chunked writer’da unit-over-registry precedence ham kafolatlanmagan.

10. **[PARTIAL] Upload preview/display.** Classifier RES parsing/preview’da ishlatiladi: `resursMkKabAniqla` name-based `БЕЗСКЛАД` ni qaytaradi (`frontend/src/admin/sahifalar/SmetaYuklaNative.tsx:382-409`), RES category review ro‘yxati va `БЕЗСКЛАД` tanlovi ko‘rsatiladi (`:1340-1377`). Biroq asosiy LRV preview faqat xom cell’larni chiqaradi (`:1242-1252`) va flatten qilingan import qatorlarida `kat` yo‘q (`frontend/src/lib/smeta-flatten.ts:39-43`); importdan oldin har bir LRV resursining predicted category’si ko‘rsatilmaydi.

11. **[PROVEN; PARTIAL main tree] Post-import vedomost display.** `БЕЗСКЛАД` kategoriya tartibiga kiritilgan (`frontend/src/lib/resurs-vedomost.ts:48`), `t2_qator_holat` qatorlari `kat` bo‘yicha guruhlanadi va jamlanadi (`:70-92,126-140`), UI esa kategoriya sarlavhasi va Smeta/F2/Qoldiq summalarini chiqaradi (`frontend/src/admin/sahifalar/ResursVedomostNative.tsx:40-75,92-102`). Oddiy `SmetaTree` qator satrida `kat` ko‘rsatilmaydi (`frontend/src/umumiy/daraxt/SmetaTree.tsx:339-353`); u faqat edit modal’ga uzatiladi (`:564-570`).

12. **[BLOCKED] Legacy direct writer va runtime.** `t2_qator_qosh` uchun source signature/body checkout’da yo‘qligi, live catalog connection ham yo‘qligi migration’ning o‘zida qayd etilgan (`supabase/migrations/20261021150000_t2_resurs_kategoriya_bezsklad_v1.sql:19-22`); gateway esa direct `qator_qosh` amalini expose qiladi (`frontend/functions/api/sb-yoz.ts:33-35`) va client `kat` ni forward qiladi (`frontend/src/api/supabase.ts:729-765`). Shuning uchun bu non-import writer’da classifier wiring’ni PROVEN/MISSING deb qat’iy yopib bo‘lmaydi. Migration source-only deb belgilangan (`.../20261021150000_t2_resurs_kategoriya_bezsklad_v1.sql:3-4`); live Supabase migration order/function definition/readback dalili yo‘q.
